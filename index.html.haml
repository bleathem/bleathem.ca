---
layout: default
---
-for post in page.posts
  #content
    .post
      %h6= post.date.strftime("%A, %B #{post.date.day}, %Y")
      %h2
        %a{:href=>post.url}= post.title
      = post.content
      %hr
    = page.posts.links